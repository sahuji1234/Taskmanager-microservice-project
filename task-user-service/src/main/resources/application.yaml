server:
  port: 5001

spring:
  application:
    name: USER-SERVICE
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/task_management
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
